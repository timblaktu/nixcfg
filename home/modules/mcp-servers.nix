{ config, lib, pkgs, ... }:

{
  # This file is kept for reference but is no longer needed.
  # Claude Code Enhanced configuration is now in base.nix via programs.claude-code-enhanced.
  #
  # The module was renamed from programs.claude-code to programs.claude-code-enhanced
  # to avoid conflict with upstream home-manager's programs.claude-code module.
  # See docs/claude-code-upstream-contribution-plan.md for migration strategy.
}
